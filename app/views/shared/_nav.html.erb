<div class="navbar navbar-fixed-top">
	<div class="navbar-inner">
	    <div class="container">
	      <div class='brand pull-left'>
		<a href='/quotes'>
			<%= image_tag "bird.png",{:width=>'30px',:height=>'20px'}%>
		</a>
		Quotes
		  </div>
		  <ul class='nav pull-right'>
			<li class='<%=@tab == 'home' ? "active" : ""%>'><a href='/quotes'>Home</a></li>
			<% if user_signed_in? %>
				<li class='<%=@tab == 'New' ? "active" : ""%>'><a href='/quotes/new'>New</a></li>
				<li><%=link_to "Logout",destroy_user_session_path, :method => :delete%></li>
			<%end%>
		  </ul>
		</div>
	</div>
</div>